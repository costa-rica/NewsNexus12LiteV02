# worker-node

- Run `npm install` before using the worker app.
- Install the browser runtime with `npm run playwright:install`.
- Start development mode with `npm run dev`.
- The default local URL is `http://localhost:8081`.
